<!DOCTYPE html>
<html lang="en-US">
  head
    title #{title}
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">

    style.
      .outer-body{
        height:80vh;
        display:flex;
        justify-content:center;
        align-items:center;
      }
      .tooltip {
        display: inline-block;
        position: relative;
        color: #2d7fc7;
        padding: 10px 25px;
        font-size: 18px;
        font-weight: bold;
        border: 2px solid #2d7fc7;
        border-radius: 35px;
        }

        .tooltip:hover {
        border-radius: 0px;
        background-color: #2d7fc7;
        color: #ffffff;
        border-radius: 35px;
        transition: 0.5s ease-in-out ;
        }

      .tooltip .top {
        min-width: 350px;
        max-height: 250px;
        position: absolute;
        top: -50%;
        left: 50%;
        transform: translate(-50%, -100%);
        padding: 10px 20px;
        font-weight:normal;
        color: #000000;
        background-color: #ffffff;
        font-size: 15px;
        border-radius: 8px;
        z-index: 999;
        box-sizing: border-box;
        box-shadow: 0px 0px 3px rgba(0, 0, 0, 0.7);
        display: none;
        transition: 0.9s all;
        }

      .tooltip:hover .top {
        display: inline-block;
      }

      .tooltip .top i {
        position: absolute;
        top: 100%;
        left: 20%;
        width: 40px;
        height: 40px;
        overflow: hidden;
      }

      .tooltip .top i::before {
        content: '';
        position: absolute;
        width: 20px;
        height: 20px;
        left: 50%;
        top: 0%;
        overflow: auto;
        z-index: 1;
        transform: translate(-50%, -50%) rotate(45deg);
        background-color: #ffffff;
        box-shadow: 0 0 3px rgba(0, 0, 0, 0.7);
      }
      p{
        text-align:justify;
      }

  <body>
    include ../include/head.pug
    include ../include/nav.pug
    .outer-body
      .tooltip Hover Me to Preview
        .top
          h3 Hello, Testing Shastra
          p Lorem ipsum dolor sit, amet consectetur adipisicing elit. Dolorem aperiam quia similique fuga, fugit perspiciatis?
          i
    include ../include/footer.pug
  </body>
</html>
